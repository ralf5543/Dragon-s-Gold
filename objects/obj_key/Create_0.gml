/// @description key variables

image_speed = 0;

can_play_sound = false;

hsp = 0; // horizontal speed
vsp = 0; // vertical speed
grv = .4; // gravity

enum KEYVALUE {
	BRASS,
	SILVER,
	GOLD
}

value = KEYVALUE.GOLD;